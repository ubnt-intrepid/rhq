# vim: ft=toml ts=2 sw=2 et :

# The path of root directory to manage cloned repositories.
root = "~/.rhq"

# supplemental lookup directories to search local repositories.
supplements = [
  "~/.go/src",
  "~/.vim/plugged",
  "$ZPLUG_REPOS",
  "~/.dotfiles"
]